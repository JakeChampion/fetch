#!/usr/bin/env ruby

require 'json'

obj = JSON.parse(ARGF.read)

test = obj['js tests'][0]

warn test['url']
warn test['platform']
warn test['result'].inspect

if test['result'] && test['result']['passed'] == test['result']['total']
  exit 0
else
  exit 1
end
